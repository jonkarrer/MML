import type { NextPage } from "next";

const Desktop: NextPage = () => {
  return (
    <header className="hidden lg:block absolute top-0 left-0 py-2">
      <nav>
        <span className="text-ty text-white font-bold">mml</span>
      </nav>
    </header>
  );
};

export default Desktop;
